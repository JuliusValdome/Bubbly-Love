/*if room == Room_Tes{
	draw_text(400, 400, string(DfTimer));

	for(var i = 0; i < array_length(passiveItems); i++){
		draw_text(900, 32 + 24 * i, string(passiveItems[i]));
	}

	for(var i = 0; i < ds_map_size(girlsLevel); i++){
		var name = listedGirls[i];
		var level = ds_map_find_value(girlsLevel, name);
	
		draw_text(100, 200 + 20 * i, name + ": " + string(level));
	}

	for(var i = 0; i < ds_map_size(itemsLevel); i++){
		var name = listedItems[i];
		var level = ds_map_find_value(itemsLevel, name);
	
		draw_text(250, 200 + 20 * i, name + ": " + string(level));
	}
	
	draw_text(700, 500, "Level: " + string(loveLevel));
	draw_text(700, 550, string(love) + " / " + string(loveNextLevel));
}*/
/*
draw_text(100, 100, string(love));
draw_text(100, 140, string(loveNextLevel));*/